# Carried-items backlog (2026-06-08)

Closes the three items the 2026-06-06 cycle carried: the cross-cutting
**no-localization** item, plus the two parts the completeness critic flagged as
never audited — **Health & Imports** and **Paywall/StoreKit**. Each was mapped
by deep exploration first; every inherited claim below was **verified against
real code before any fix** (per `ios-audit-finding-false-positives` /
`verify-audit-claim-before-implementing`). Two "high" findings disproved on
inspection — recorded so they aren't re-opened.

Scope decisions (owner): localization stays **English-only / extraction-ready**
(no translations yet); `proRequired` stays **false** (flipping it + creating App
Store Connect products are out of scope — external deps).

**Status: B + C complete; A foundation + UI subset complete (Coach-coupled
labels deferred — see below). Gate green: 941 unit tests, 0 failures;
exportLocalizations 454 trans-units, no `pt_*` leakage.**

---

## Workstream B — Health & Imports

- [x] **B1 — Warmup rows skipped without a count** (`Imports/FitbodCSVParser.swift`).
  `warmupsSkipped` now threaded through `ParseResult` → `Result` → surfaced in
  the CSV import summary. Parser test asserts the count. `c19c27f`.
- [x] **B2 — "Last import: N sets" reads as net-new** → honest "read N sets"
  copy. `c19c27f`.
- [x] **B3 — Body-fat % ingested unvalidated** → `BodyMetricsMerger` drops
  weight ≤ 0 and BF ≤ 0 / > 75 before merge; merger tests added. `d58e379`.
- [x] **B4 — Batch insert COMMITs partial on mid-loop failure** → track step
  success, `ROLLBACK` on failure across insert/insert/delete. `10a573c`.
- [x] **B5 — Zero-results-after-sync ambiguous** → post-sync empty state now
  points at Settings → Health → Data Access. `3adc6f6`.
- [x] **B6 — Backfill→merge dedup untested** → merger tests for the backfilled
  scalar inside/outside the 60 s window. `d58e379`.
- [x] **B7 — HK→DB persistence "untested" — FALSE POSITIVE.**
  `ImportedWorkoutsDatabaseTests.swift` already covers it. No work.
- [x] **B8 — CSV timezone — VERIFIED CORRECT.** Absolute-instant storage. No work.

## Workstream C — Paywall/StoreKit (`proRequired` stays false)

- [x] **C1 — No `.storekit` config** → added `PhaseTraining.storekit`
  (pro_monthly) + wired into the run scheme via `Project.yml`. `df4abb4`.
- [x] **C2 — Entitlement decision untestable** → extracted pure
  `SubscriptionStore.isEntitled(by:now:)` + 8-case test matrix. `817dbed`.
- [x] **C4 — Paywall error had no recovery** → "Try again" button re-fires
  `refresh()`. `f61f4c1`.
- [x] **C5 — "Manage in Settings" was prose** → `.manageSubscriptionsSheet`
  button. `f61f4c1`.
- [x] **C6 — Flip-day checklist stale** → folded offline-`proKey` + `.storekit`
  notes into `CoachEntitlement.swift`. `8b3f94c`.
- [x] **C3 — Subscription row "blank" — FALSE POSITIVE.** Already shows
  "Pro"/"Upgrade". No work.

## Workstream A — Localization readiness (English-only)

- [x] **A1 — Infra** → `SWIFT_EMIT_LOC_STRINGS` + `LOCALIZATION_PREFERS_STRING_CATALOGS`
  (app target) + `Localizable.xcstrings`. ~731 `Text/Button/Label` literals
  auto-extract; verified 454 trans-units via `-exportLocalizations`. `f018fc2`.
- [x] **A3/A4 (prompt-free subset)** → wrapped the String-typed labels that do
  NOT auto-extract and are NOT Coach-prompt-consumed: muscle/division/movement
  filters, library/difficulty/focus-preset enums, and the WeeklyCheckIn intent
  chips + feedback ratings + soreness energy/severity tuples. `9b8ef54`.
- [ ] **A2 + A3 remainder (DEFERRED — needs the Coach-prompt decouple first).**
  The bulk of the 108 enum `.label`s (`DayKind`, `Focus`, `Experience`,
  `Gender`, `Equipment`, `StartingState`, `SeasonPhase`, `EventIntensity`,
  `StrengthTier`, `BigLift`, `Screen`) are consumed by `CoachContext` to build
  the LLM prompt. **Gate: before localizing them OR adding any non-English
  translation, give the Coach prompt a language-stable label source** (switch
  those prompt sites to `.rawValue`, or add a `promptLabel`). English-only ships
  fine without this (no translations ⇒ English base everywhere), but a German
  bundle would otherwise leak into the prompt. Verified `CoachContext` consumes
  these labels; `weeklyCheckIns`/`.intent` are NOT prompt-consumed (which is why
  the check-in tuples above were safe to wrap).
- [ ] **A5–A7 (DEFERRED — polish).** Composed-sentence format strings +
  `%lld` plural variations (`PlanValidator`, count ternaries); component
  call-site audit (TabHeader/SettingsRow/InsightCard literal args);
  `Text(verbatim:)` for "·"/"—" + `shouldTranslate:false` for catalog noise keys
  ("", "0", "date").

## What NOT to touch (load-bearing)

- `Coach/CoachSystemPrompt.swift` — LLM system prompt, English by contract.
- Enum `rawValue`s / slugs, UserDefaults keys (`pt_*`), SQL, asset names,
  `accessibilityIdentifier`s — internal, must stay English.
- `coach.db` content (DB-sourced names) — not catalog.
- `PhaseTraining/Resources/coach.db`, `db/source/**`, `scripts/db/**` — owned by
  the concurrent substitute-data backfill session.
- `CoachEntitlement.proRequired = false` — held-open gate; locked by
  `test_shippedState_isFree`.

## Test gate (this cycle)

`xcodebuild test` (serial) — 941 unit tests, 0 failures, 2 pre-existing skips.
`xcodebuild -exportLocalizations` — 454 trans-units; sentinels present; `pt_*`
keys + CoachSystemPrompt phrases absent from the XLIFF.
