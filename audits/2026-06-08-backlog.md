# Carried-items backlog (2026-06-08)

Closes the three items the 2026-06-06 cycle carried: the cross-cutting
**no-localization** item, plus the two parts the completeness critic flagged as
never audited — **Health & Imports** and **Paywall/StoreKit**. Each was mapped
by deep exploration first; every inherited claim below was **verified against
real code before any fix** (per `ios-audit-finding-false-positives` /
`verify-audit-claim-before-implementing`). Two "high" findings disproved on
inspection — recorded so they aren't re-opened.

Scope decisions (owner): localization stays **English-only / extraction-ready**
(no translations yet); `proRequired` stays **false** (flipping it + creating App
Store Connect products are out of scope — external deps).

---

## Workstream B — Health & Imports

- [ ] **B1 — Warmup rows skipped without a count** (`Imports/FitbodCSVParser.swift:138`).
  Verified: `if isWarmupRaw == "true" { continue }`, no tally; `ParseResult`
  has no warmup field. Fix: count skipped warmups, thread through
  `WorkoutCSVImporter.Result`, surface in `CSVImportSection` last-import block.
  Test: parser reports the count.
- [ ] **B2 — "Last import: N sets" reads as net-new, is parsed-count**
  (`CSVImportSection.swift:105`; `setsInserted = parsed.sets.count`, and the
  table is INSERT OR REPLACE so a re-import shows the same N with 0 net new).
  Fix: honest copy ("read N sets").
- [ ] **B3 — Body-fat % ingested unvalidated** (`HealthKitImporter.swift:211`
  scales 0–1 → 0–100). HK constrains its own writes, but third-party apps can
  write garbage to HK. Fix: drop implausible body samples (weight ≤ 0, BF ≤ 0
  or > 75) in the pure `BodyMetricsMerger`. Test: out-of-range sample dropped.
- [ ] **B4 — Batch insert COMMITs partial data on mid-loop step failure**
  (`UserDatabase+Imports.swift` `insertImportedWorkouts`/`insertImportedSets`;
  `defer { COMMIT }` runs even if a `sqlite3_step` failed — contradicts the
  documented "all rows or none"). Fix: track step failures, `ROLLBACK` instead
  of `COMMIT` on failure. Verified the prepare-fail path is benign (BEGIN then
  COMMIT no-op), so only the partial-commit path needs the guard.
- [ ] **B5 — Zero-results-after-sync is ambiguous (denied vs no data)**
  (`HealthWorkoutSyncSection.swift:71` "No imported workouts yet"). HK won't
  expose read-auth status by design. Fix: after a sync attempt that yields 0
  rows, add a hint pointing at Settings → Health → Data Access.
- [ ] **B6 — Backfill→merge dedup-window interaction untested.** Add a merger
  test: a pre-seeded weight log (the post-`backfillBodyWeightLogFromScalar`
  state) + an HK sample inside vs. outside the 60 s window; pin "inside → skip,
  outside → add".
- [x] **B7 — HK→DB persistence "untested" — FALSE POSITIVE.**
  `ImportedWorkoutsDatabaseTests.swift` already covers insert/fetch round-trip,
  window exclusion, idempotency-on-id, and summary empty/populated. No work.
- [x] **B8 — CSV timezone — VERIFIED CORRECT.** `FitbodCSVParser` parses the
  explicit `+0000` offset and stores absolute epoch seconds; display is local.
  Absolute-instant storage is right — no off-by-N bug. No work.

## Workstream C — Paywall/StoreKit (`proRequired` stays false)

- [ ] **C1 — No `.storekit` config file** (verified: `SubscriptionStore` /
  `PaywallView` comments both call it out as missing). Add `PhaseTraining.storekit`
  with the `pro_monthly` auto-renewable, wire it into the run scheme
  (`Project.yml` → `xcodegen generate`) so PaywallView previews resolve products.
- [ ] **C2 — Entitlement decision untestable** (lives inline in the private
  `refreshEntitlement`, reads real `Transaction.currentEntitlements`). Extract a
  pure evaluator (productID/verified/revoked/expiration → Bool) and unit-test it
  exhaustively. (SKTestSession purchase-flow tests are flaky headless — out of
  scope; the pure evaluator is the reliable seam, per the plan's fallback.)
- [ ] **C4 — Paywall error state has no recovery** (`PaywallView` shows
  `lastError`, no retry). Add a Retry button that re-fires `refresh()`.
- [ ] **C5 — "Manage in Settings" is prose, not an affordance.** Replace with a
  real `.manageSubscriptionsSheet`-backed button.
- [ ] **C6 — Flip-day checklist is stale.** Fold the deferred findings
  (offline-first-launch leaves `proKey` unpopulated; lapsed-user bubble vanishes
  silently) into `CoachEntitlement.swift` so flip day inherits them.
- [x] **C3 — Subscription row "blank" — FALSE POSITIVE.** `subscriptionRowValue`
  already returns "Pro"/"Upgrade". Renewal-date display needs StoreKit renewal
  info not fetched while the gate is held open — speculative, skipped. No work.

## Workstream A — Localization readiness (English-only) — runs LAST

So B/C's new strings land in the catalog. Per the validated design:

- [ ] **A1 — Infra**: `SWIFT_EMIT_LOC_STRINGS` + `LOCALIZATION_PREFERS_STRING_CATALOGS`
  on the app target only; seed `Localizable.xcstrings` + `InfoPlist.xcstrings`
  (`sourceLanguage: en`). `xcodegen generate`; baseline export.
- [ ] **A2 — Decouple LLM prompts FIRST**: `Coach/CoachContext.swift` consumes
  enum `.label` for prompt text → switch those sites to `.rawValue` before any
  label is localized. Re-grep `Coach/` after each enum file.
- [ ] **A3 — Enum `.label` → `String(localized:)`** (~108 vars across
  ExerciseFilters, WeekPlan, WeekOverrides, TrainingMemory, screen-local enums).
  The ~731 `Text("…")` literals need no change (LocalizedStringKey auto-extracts).
- [ ] **A4 — Tuple arrays**: wrap display half, internal key untouched
  (intentChips, energy/severity/timeBudget, ratings). Disambiguate genuine
  collisions ("High") with explicit keys, sparingly.
- [ ] **A5 — Builders + plurals**: `PlanValidator` composed sentences → single
  reorderable format strings w/ `localizedLowercase`; count ternaries →
  `%lld …` + catalog plural variation.
- [ ] **A6 — Component call-site audit**: TabHeader/SettingsRow/InsightCard/chip
  take `String` — wrap *literal* args at call sites; do NOT change param types.
- [ ] **A7 — Cleanup**: `Text(verbatim:)` for "·"/"—"; `localizedUppercase` on
  localized eyebrows; `shouldTranslate:false` for residual noise keys.

## What NOT to touch (load-bearing)

- `Coach/CoachSystemPrompt.swift` — LLM system prompt, English by contract.
- Enum `rawValue`s / slugs, UserDefaults keys (`pt_*`), SQL, asset names,
  `accessibilityIdentifier`s — internal, must stay English.
- `coach.db` content (551+ exercise/routine names) — DB-sourced, not catalog.
- `PhaseTraining/Resources/coach.db`, `db/source/**`, `scripts/db/**` — owned by
  the concurrent substitute-data backfill session. Never stage these;
  `git pull --rebase` before push.
- `CoachEntitlement.proRequired = false` — the held-open gate; locked by
  `test_shippedState_isFree`.

## Test gate (before push)

Full serial `xcodebuild test` (per `Project.yml` parallelizable:false note),
then `xcodebuild -exportLocalizations` coverage check: ≥600 trans-units,
sentinels present (`Chest`, `%lld %@ days this week`, `%lld exercises`),
must-not-localize ABSENT (grep XLIFF for a `pt_` key + a CoachSystemPrompt
phrase — both absent).
