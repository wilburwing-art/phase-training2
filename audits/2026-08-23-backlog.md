# 2026-08-23 hardening backlog

Source: 97-agent ultra critique (wf_35cd5ad4-08c), 213 findings across 9 parts.
Every high-severity and absence claim below was grep-verified by an independent
skeptic agent before making this list — but **per-item re-verification still
applies at implementation time** (`verify-audit-claim-before-implementing`,
`ios-audit-finding-false-positives`): verify the load-bearing factual premise
against the code you're about to edit before writing the fix.

## How to run this

Top-down. Tier 0 first, straight to main, one commit per item. Tier 1 next.
Tier 2 items each start with a **verify** step — do the grep/read before
acting; if the verify fails, tick the box with a `FALSE-POSITIVE:` note and
move on. Tier 3 is REPORT-ONLY this cycle (see
`audits/2026-08-23-architecture-report.md`) — no code edits without the test
gate. Tier 4 is a separate sitting; leave unchecked.

Build gate between items: `xcodebuild test` on the unit suite (grep the full
log on failure — see `phase-training-xcodebuild-test-failed-grep-full-log`).
New files need 4 hand edits in project.pbxproj (`pbxproj-add-files-by-hand`).

---

## Tier 0 — ship blockers / security / data loss

**Status 2026-08-24: 9 of 10 landed, T0-1 partial (needs Wilbur, external).**
All implemented inline rather than by agent fan-out — two workflow attempts
died on usage limits having landed zero edits. Each item is one commit,
build- and test-verified; 969 unit tests green.

> **T0-1 ACTION REQUIRED (outside this repo).** The in-repo half landed (daily
> gateway ceiling). The token still ships inside the IPA and the gateway is
> BYOK, so: **(1)** rotate `PHASETRAINING_CF_AIG_PROD_TOKEN` — assume the
> current one is recoverable from any distributed build; **(2)** set rate +
> spend limits on the Cloudflare AI Gateway itself, which is the only ceiling
> a token-extractor can't bypass; **(3)** longer term, mint per-install tokens
> server-side. No client-side change can substitute for (2).

- [x] **T0-1 Gateway token compiled into the binary.** *(partial — in-repo half done, `72c616c`+. Verified the file is gitignored and was never committed. Added `CoachConfig.dailyRequestCeiling` enforced in `CoachClient` on both send and stream paths, covering all four callers. External steps above remain open.)* `PhaseTraining/Generated/CoachSecrets.swift` embeds a live Cloudflare AI Gateway token as a plaintext string sent as `cf-aig-authorization` (PhaseTraining/Coach/CoachClient.swift:77). Gateway is BYOK, so the IPA is an unmetered Claude proxy billed to Wilbur; the only turn caps live in client UserDefaults. Fix: rotate the token, and gate the gateway path on a server-checked constraint (minimum: per-token rate limit + spend cap configured on the CF gateway itself; do NOT try to hide the string better and call it done). Verify what CI injects into Generated/ before touching.
- [x] **T0-2 Erase-all resurrects wiped data.** BackupCoordinator.eraseAllData (PhaseTraining/Data/BackupCoordinator.swift:93) wipes `pt_` keys but SportLogStore / RecentPicksStore / CoachConversationStore are app-lifetime `@StateObject`s whose in-memory arrays write themselves back on next mutation. Fix: give each store a `reset()` and call it from the coordinator. *(DONE `4ed6b4f` — added SportLogStore.reset() + CoachConversationStore.resetAll(); RecentPicksStore.clear() already existed and was simply never called. Also fixed the ProfileScreen preview.)*
- [x] **T0-3 Backup envelope loses history on phone switch.** BackupManager (PhaseTraining/Data/BackupManager.swift:29) omits sport logs, missed-workout log, recent picks, coach conversations, and the entire `imported_workouts`/`imported_sets` SQLite history; restore also only clears the 7 keys it writes, mixing old device data into the restored state. Fix: extend the envelope (bump its version), and make restore's destructive contract actually destructive. *(DONE `(envelope v2)` — added sportLogs/missedWorkouts/recentPicks/importedWorkouts/importedSets + clearAllImports. Needed an explicit init(from:): Swift ignores property defaults for absent keys, so the `= []` defaults alone would have made every EXISTING v1 backup un-restorable. 3 new tests.)*
- [x] **T0-4 Silent DB failure fakes a successful save.** UserDatabase (PhaseTraining/Data/UserDatabase.swift:51) nulls `db` on open/migration failure and every write silently no-ops; SessionStore.saveCompleted still reports success, so the workout looks saved until next launch. Fix: propagate open/migration failure to a visible error state (banner + disable save), and make `saveCompleted` surface write failure. *(DONE `72c616c` — UserDatabase.unavailableReason on open/migration failure, saveSession returns Bool, SessionStore.persistenceFailed latches, RootTabView alerts. Also fixed the RootTabView preview.)*
- [x] **T0-5 Consent surface is factually wrong + pre-ticked.** Three linked edits, one commit: (a) OnboardingCoachConsentScreen:21 pre-selects consent (`localOn = true`) — default it OFF and make enabling an affirmative tap; (b) OnboardingCoachConsentScreen:38 says "No identity info is sent" while CoachContext ships age/gender/height/weight/injuries — rewrite the copy from `CoachConsent.modalBody` (single source, per CoachConsent.swift:19) and make modalBody name body metrics + injuries; (c) CoachConsent.swift:24 modal understates the payload the same way. Include the privacy-policy link CoachConsent.swift:6 promises (docs/privacy.md exists; needs a hosted URL). *(DONE `5b761be` — consent no longer pre-ticked (Continue gated on an affirmative pick) and modalBody now itemizes body metrics + injury notes instead of claiming "No identity information is sent".)*
- [x] **T0-6 One-tap deletion of a decade of import history.** CSVImportSection delete button (PhaseTraining/Screens/HealthImports/CSVImportSection.swift:90) calls `deleteImports(source:)` with no confirmation. Fix: confirmationDialog with the row count in the message, matching the erase-all pattern in the same part. *(DONE `485224b` — confirmationDialog naming the source and exact set count.)*
- [x] **T0-7 Two ski sports generate 0-exercise workouts.** `snow-sports` and `ski-mountaineering` route to the season engine but have zero rows in coach.db `sport_movements` (PhaseTraining/Data/SportSeason/SportSeasonGenerator.swift:91); no empty-pool guard exists, so every lift day is "0 movements · ~0 min". Fix: empty-pool guard that falls back (alias both slugs to the alpine-skiing pool is the cheap correct fix), plus extend `test_catalog_pools_are_healthy` to iterate `Sport.catalog.filter(SportCatalog.isPlannable)` (see PhaseTrainingTests/SeasonFidelityTest.swift:21). *(DONE `d029327` — added SportSeasonGenerator.poolSlug(for:) aliasing ski/climbing variants onto the two seeded pools; 2 new CI gates that iterate what a user can actually pick. Note: the 6 climbing variants are absent from Sport.catalog, so the live defect was the 2 ski slugs only.)*
- [x] **T0-8 Weekly Check-In plans the wrong week and then loses the answers.** (a) WeeklyCheckInFlow:156 calls `Planner.generate` without `today:`, anchoring on the CURRENT Monday while every string says "next week"; (b) WeeklyCheckInFlow:185 writes overrides stamped with the current weekStart, which PlanStore discards as stale on Monday (PlanStore.swift:190/286). Fix both together: anchor generation and overrides on next week's start when invoked from the check-in. *(DONE `742fb6c` — generates against next week and stages via new PlanStore.stagePlan/promotePendingIfDue rather than blanking the in-flight week. 4 new tests.)*
- [x] **T0-9 Swap keeps the old exercise's identity, history, and unit.** Two sites, one commit: TodayScreen+TemplateEditor.swift:233 keeps `old.id`/`old.unit` (session pre-fills the replaced lift's 225lb history under the new name; bw/barbell unit mismatch breaks the weight column) and LogScreen.swift:120 rewrites only name/type leaving `prevSets`/`unit` (progression pill prescribes a load from a different lift — safety-relevant). Fix: derive id/unit from the picked exercise, clear prevSets on mid-workout swap. *(DONE `4d775f4` — both swap sites now derive id/unit from the picked exercise; mid-workout swap re-resolves prevSets by name.)*
- [x] **T0-10 Demo seed writes to the real active-session key, outside `#if DEBUG`.** PhaseTrainingApp.swift:181 `seedSupersetsDemo` contradicts the comment above it and is the only seed not DEBUG-gated. Fix: move inside the DEBUG block (keep `--seed-supersets-demo` behavior identical for XCUITests — see `phase-training-xcuitest-recipe`). *(DONE `040fab7` — moved inside #if DEBUG and corrected the false "separate UserDefaults suite" comment.)*

## Tier 1 — user-facing bugs (one screen each)

**Status 2026-08-25: ALL 59 landed, 972 unit tests green.** Implemented inline
in seven file-disjoint groups, one commit per item or tight cluster, build +
test gate per group. Deviations from the audit's prescription are noted in the
commit messages; the notable ones: T1-15 (counting a default-ON filter was
rejected — the existing test encoded the right contract), T2-13 (`detailExercise`
was not dead code, it was missing its consumer, which T1-14 supplied), T1-42
(six sites, not four), T1-51 (the reported in-season-alpine case was actually
ski-mountaineering), T1-44 (already closed by T0-1's daily ceiling).

### Today + Log
- [x] **T1-1** PR badge on completion summary can never light: `prIds` keyed `"name|reps"` vs `ex.id` slug (PhaseTraining/Screens/CompleteScreen.swift:299). Key both sides the same.
- [x] **T1-2** Rest-timer expiry is silent when the phone is locked — no UNUserNotification is ever scheduled (PhaseTraining/Screens/RestTimerState.swift:125). Schedule one on set-complete, cancel on foreground expiry.
- [x] **T1-3** Finish commits irreversibly with no confirmation while sitting next to "Log all" (PhaseTraining/Screens/LogScreen.swift:257); Cancel gets an alert but Finish doesn't. Add confirm when any set is incomplete.
- [x] **T1-4** LogScreen nil-active fallback fabricates `upper-1` instead of today's plan (PhaseTraining/Screens/LogScreen.swift:491). Read planStore or render empty state.
- [x] **T1-5** Rest/event days strand the user with no CTA (PhaseTraining/Screens/TodayScreen.swift:517). Add a "train anyway" affordance routing to OverrideTodaySheet.
- [x] **T1-6** Inline Today edits silently lost on teardown (PhaseTraining/Screens/TodayScreen+TemplateEditor.swift:271) — persist to the day's override, not just @State.

### Week + Check-In
- [x] **T1-7** "Got it" on the validation banner visibly does nothing (PhaseTraining/Screens/PlanValidationBanner.swift:41) — add per-week dismiss on top of the 3-week suppression learner.
- [x] **T1-8** "Mark as travel day" clobbers an existing race/sport with no confirm, unlike its two sibling actions (PhaseTraining/Screens/WeekDayEditSheet.swift:293). Route through `requestAdd`.
- [x] **T1-9** Check-in events screen appends duplicate same-day events; Planner takes `.first` (PhaseTraining/Screens/WeeklyCheckIn/CheckInEventsScreen.swift:44). Add the replace-confirm WeekDayEditSheet already has.
- [x] **T1-10** "Mark off this week — Forces rest for today only" subtitle is wrong for any non-today row (PhaseTraining/Screens/WeekDayEditSheet.swift:207). Fix the copy to name the tapped day.
- [x] **T1-11** Check-in steps 2-5 have no close affordance (PhaseTraining/Screens/WeeklyCheckIn/WeeklyCheckInFlow.swift:99) — pass `onClose` everywhere.
- [x] **T1-12** Check-in TONE chips are inert; WeekScreen's `weekTone` is the wired control (PhaseTraining/Screens/WeeklyCheckIn/CheckInIntentScreen.swift:30). Wire deload/volume chips to `overrides.weekTone`; travel chip to unavailable-days. (Pairs with T2-1.)
- [x] **T1-13** IntensityEditorSheet + EventEditorSheet hint copy contradicts Planner's actual taper rules (PhaseTraining/Screens/WeekDayEventSheets.swift:367, :154). Make the hints conditional on event kind, matching Planner.swift:598-661.

### Library
- [x] **T1-14** No global exercise search; 19 catalog rows unreachable from any muscle tile (PhaseTraining/Screens/LibraryScreen.swift:74). Enable the search bar on the Exercises segment backed by the ExercisePickerSheet query.
- [x] **T1-15** `hideOtherSports` hides ~44% of the catalog with no toggle anywhere (PhaseTraining/Screens/ExerciseFilterSheet.swift:45) and is omitted from `secondaryCount`/`clearSecondary` (PhaseTraining/Data/ExerciseFilters.swift:274). Add a "Show all sports" row to the filter sheet + count it. Then make the eyebrow count honest (LibraryScreen.swift:120).
- [x] **T1-16** `binding(forId:)` falls back to `exercises[0]` — traps on empty, edits the wrong row otherwise (PhaseTraining/Screens/CustomRoutineEditSheet.swift:271). Return a safe binding.
- [x] **T1-17** Nil image URL renders the broken-photo glyph for 144 exercises (PhaseTraining/Components/CachedAsyncImage.swift:50) — nil URL is placeholder, not failure. Also reset state on url change (:49).
- [x] **T1-18** Detail hero ignores the bundled WebP the thumbnail already resolved (PhaseTraining/Screens/ExerciseDetailSheet.swift:270). Resolve bundle-first like ExerciseThumbnail.
- [x] **T1-19** Replace picker inside custom-routine edit opens unfiltered, unlike every sibling swap surface (PhaseTraining/Screens/CustomRoutineEditSheet.swift:69). Pass `ExerciseFilters.similar(toExerciseNamed:)`.

### Progress + History
- [x] **T1-20** StrengthStandards matcher triple: leading-space `" squat"` misses "Squat (Barbell)" (PhaseTraining/Data/StrengthStandards.swift:48); hack/front/box squats count as Back Squat (:64); dumbbell/incline/close-grip count as Bench while OHP filters them (:62). One table-driven fix + unit table test.
- [x] **T1-21** Progress tab fully gated on lift history — body weight/comp/recovery invisible until first workout (PhaseTraining/Screens/ProgressScreen.swift:79). Gate per-card, not per-tab.
- [x] **T1-22** "FRESH MUSCLE GROUPS" counts 80 rows incl. never-trained; list below shows 19 (PhaseTraining/Screens/ProgressRecoverySection.swift:35). Count what the list shows.
- [x] **T1-23** History drops bodyweight sets — completed sets with empty weight render "—" (PhaseTraining/Screens/HistoryScreen.swift:295). Respect `bodyweightUnit`.
- [x] **T1-24** History counts warmups in TOTAL SETS; every Progress aggregate excludes them (PhaseTraining/Screens/HistoryScreen.swift:179). Align on `done && !isWarmup`.
- [x] **T1-25** Body-weight sheet double-logs same-day duplicates from the prefilled field (PhaseTraining/Screens/BodyWeightLogSheet.swift:309). Same-day upsert.
- [x] **T1-26** BodyAnatomyView hard-codes `.male` for every user (PhaseTraining/Components/BodyAnatomyView.swift:91). Pass `TrainingMemory.gender`.
- [x] **T1-27** Recovery row thumbnails use the screen-wide side toggle so anterior rows render blank (PhaseTraining/Screens/ProgressRecoverySection.swift:143). Pick side per-muscle.
- [x] **T1-28** Pull-up card shows effective weight (load+BW) as if the user logged it (PhaseTraining/Data/StrengthStandards.swift:221). Render the logged set; keep effective weight for the calc.
- [x] **T1-29** Streak while-loop can spin forever on nil date arithmetic (PhaseTraining/Screens/Progress/ProgressScreen+StatCards.swift:172). Bound the loop.
- [x] **T1-30** Tier labels: `.nonbinary`/`.preferNotToSay` silently routed to female thresholds while copy says gender is needed (PhaseTraining/Data/StrengthStandards.swift:143). Disclose the curve or show unlabeled.

### Profile + editors + imports
- [x] **T1-31** Deselecting a sport orphans `seasonsBySport`; Planner books placeholder days forever with no reachable UI (PhaseTraining/Screens/SportsEditorSheet.swift:73 → Planner.swift:722). Clear the dict entry on deselect + membership-check in Planner.
- [x] **T1-32** Support pattern survives primary-sport switch with no UI to turn it off (PhaseTraining/Screens/ProfileScreen.swift:111 → Planner.swift:768). Gate `applySupportPattern` on ski primary, or always show the row when a pattern exists.
- [x] **T1-33** Injury search misses body regions — "knee" returns 0 despite 8 knee injuries (PhaseTraining/Screens/InjuryPickerSheet.swift:32). Filter name OR bodyRegion.
- [x] **T1-34** Dislikes placeholder "e.g. burpees" can never match singular "Burpee" (PhaseTraining/Screens/DislikesEditorSheet.swift:27). Show a match count against the catalog when the chip is added; fix example.
- [x] **T1-35** Peak-date picker shows today when nil and can't set today (PhaseTraining/Screens/SeasonsEditorSheet.swift:108). Explicit "Set date" state.
- [x] **T1-36** Equipment Custom mode collapses on first chip tap — twice (PhaseTraining/Screens/EquipmentEditorSheet.swift:46 AND PhaseTraining/Screens/Onboarding/OnboardingEquipmentScreen.swift:56, same derivation bug). Track chosen tier as state, not by array-equality inference.
- [x] **T1-37** Export-backup presents a sheet over a sheet — share sheet fails to present (PhaseTraining/Screens/DataEditorSheet.swift:24). Present after dismiss completes.
- [x] **T1-38** Paywall empty state shows developer instructions to TestFlight users (PhaseTraining/Screens/PaywallView.swift:155 via ProfileScreen.swift:191). User-facing empty copy; hide the row while no products exist. Add ToU/privacy links to `legalFinePrint` (:167) while in the file.
- [x] **T1-39** Health sync errors render raw `"\(error)"` interpolations (PhaseTraining/Screens/HealthImports/HealthWorkoutSyncSection.swift:183). LocalizedError-first like CSVImportSection.
- [x] **T1-40** "N rows skipped (see Xcode console)" — nothing is ever logged; parseErrors carries reasons that die unused (PhaseTraining/Screens/HealthImports/CSVImportSection.swift:126). Show the reasons in-app.

### Coach
- [x] **T1-41** Raw gateway/config errors rendered verbatim as coach speech (PhaseTraining/Coach/CoachDrawer.swift:413). Map to a 3-case user taxonomy (offline / rate-limited / try again); keep detail behind a disclosure.
- [x] **T1-42** Every yyyy-MM-dd formatter lacks `en_US_POSIX` — non-Gregorian devices break plan-edit matching and day rollover (PhaseTraining/Coach/CoachTools.swift:451, MiniWorkoutDiffCard.swift:106, CoachDrawer.swift:28, CoachConversationStore.swift:199). One shared static formatter.
- [x] **T1-43** Tool-call JSON decode failure is silent — coach claims it proposed a change, no card renders (PhaseTraining/Coach/CoachDrawer.swift:373). Render an inline "couldn't build that proposal — retry" row.
- [x] **T1-44** Turn caps only count drawer sends; build-workout, insights, and refinement bypass the cost guard (PhaseTraining/Coach/CoachConfig.swift:33). Count all gateway callers.
- [x] **T1-45** Truncated replies indistinguishable from complete ones — `stop_reason` never read (PhaseTraining/Coach/CoachConfig.swift:25). Read it; show a "continue" affordance on max_tokens.
- [x] **T1-46** MiniWorkoutDiffCard lacks the re-entry guard its sibling documents as a watchdog fix; post-apply re-resolve renders a contradictory noop notice (PhaseTraining/Coach/MiniWorkoutDiffCard.swift:118); MiniPlanDiffCard/MiniWorkoutDiffCard also lack the double-tap `isApplying` guard (MiniPlanDiffCard.swift:122). Copy the guards across.
- [x] **T1-47** Insight sanitizer keeps only the LAST sentence of a 2-sentence insight the prompt requires (PhaseTraining/Coach/InsightGenerator.swift:129). Truncate, don't decapitate.
- [x] **T1-48** Uncensored coach insight text renders in Today's hero caption with no consent/entitlement check (PhaseTraining/Screens/TodayScreen+Derived.swift:110); prune `coachInsights` on consent revocation.

### Engine + persistence
- [x] **T1-49** Every pinned/adopted lift day generates the byte-identical session — `makeOverrideSlot` hardcodes `liftIndex: 0` (PhaseTraining/Data/Planner.swift:394). Thread the real index.
- [x] **T1-50** Season titles hardcode "Ski · …" for every sport and poison PlanValidator.resolveFocus (PhaseTraining/Data/SportSeason/SportSeasonGenerator.swift:267). Derive from sport.
- [x] **T1-51** WeeklyShape has no alpine-skiing offSeason/maintenance/eventPrep entries and no ski-mountaineering at all — in-season skier can get zero ski days (PhaseTraining/Data/WeeklyShape.swift:100). Fill the table.
- [x] **T1-52** Empty-demand slots silently dropped with no backfill — sessions shrink below the 3-movement floor (PhaseTraining/Data/SportSeason/SportSeasonGenerator.swift:68). Backfill from remaining demands.
- [x] **T1-53** Injury redistribution routes half the freed weight to `.antagonist`, which the ski pool doesn't have (PhaseTraining/Data/SportSeason/SportSeasonGenerator.swift:125). Redistribute across demands that exist in the pool.
- [x] **T1-54** Missed-workout detection ignores sport logs — completed climbing day flagged missed (PhaseTraining/Data/MissedWorkoutAutopilot.swift:45). Pass sportLogStore sessions into detect.
- [x] **T1-55** Inactivity reminder can land after workout finish — schedule/cancel race (PhaseTraining/Data/InactivityReminderScheduler.swift:31). Generation token.
- [x] **T1-56** Canonical wipe leaves the inactivity notification scheduled (PhaseTraining/Data/MemoryStore.swift:166). Cancel it in the wipe.
- [x] **T1-57** Support-sport day silently vanishes when its weekday isn't an unprotected rest (PhaseTraining/Data/Planner.swift:774). Surface a "couldn't place your climb day" note in the plan.
- [x] **T1-58** Sheet-in-`if let` presents blank sheets when the optional is nil at present time (PhaseTraining/Screens/WeekDayEditSheet.swift:103). Use `.sheet(item:)`.
- [x] **T1-59** Two dead previews trap on missing environment objects (PhaseTraining/Screens/ProfileScreen.swift:536, PhaseTraining/App/RootTabView.swift:102). Inject the missing stores.

## Tier 2 — half-finished feedback loops (VERIFY, then act)

**Status 2026-08-26: ALL 14 landed, 972 unit tests green.** Every verify step
held — each was a genuine built-but-unconsumed case. Four resolved differently
from the prescription, all recorded in the commits: T2-6 (the audit said "the
parse layer exists"; it doesn't — fixed the real CRLF bug instead), T2-8
(wiring `SupportScheduler.schedule` is a Tier-3 placement swap, so the gap is
now documented in the header + architecture report), T2-2 and T2-11 (parked
adaptive layer — gated behind an off-by-default PARAMETER, never deleted, so
their tests keep the code honest for unparking).

- [x] **T2-1 Weekly check-in intent/rating are write-only.** VERIFY: grep consumers of `weeklyCheckIns` beyond declaration/Codable/append/tests (claim: none — pre-verified but re-check). Then: wire intent → `weekTone`/regeneration inputs (with T1-12), pass `lastWeekRating` into planner feedback, and add the retention trim the field lacks (PhaseTraining/Data/TrainingMemory.swift:144). If wiring is out of scope for one sitting, stop blocking `nextEnabled` on a rating that has no effect.
- [x] **T2-2 LLM refinement is a token-burning no-op that stamps a false claim.** VERIFY: confirm `generateLift` ignores `strategy`/`hashSeed` on the season path (PhaseTraining/Data/PlanStore+LLMRefinement.swift:233, WorkoutGenerator.swift:95) — this intersects the PARKED adaptive layer (see memory: season engine sidelines adaptive layer; direction is authored spines). Cheapest correct fix: stop kicking off refinement on the season path and stop stamping `refinedByLLMAt` (kills T2-2's spend, Onboarding's silent plan rewrite at OnboardingFlow.swift:115, the refined-day variety bug at :233, and the wrong-day stamp race at :271 in one move). Do NOT try to wire strategy through the engine this cycle.
- [x] **T2-3 Dead CoachContext blocks.** VERIFY: no production caller passes `pastPlans`/`planIssues`/`missedWorkouts` (PhaseTraining/Coach/CoachContext.swift:31). They're tested and PlanStore publishes the data — wire them into CoachDrawer's snapshot call. If token budget is a concern, wire `missedWorkouts` only (smallest, most conversationally useful).
- [x] **T2-4 Coach archives are written, never readable.** VERIFY: nothing reads `pt_coach_archive_*` (PhaseTraining/Coach/CoachDrawer.swift:73). Add a minimal history list in the drawer (read-only), and a confirm on CLEAR.
- [x] **T2-5 Backup staleness check implemented, never consulted.** VERIFY: only tests reference `isMemorySchemaStale` (PhaseTraining/Data/BackupManager.swift:42). Surface the notice in BackupCoordinator's restore confirmation alert.
- [x] **T2-6 Hevy/Strava: dead branch + promise copy.** VERIFY: `detectSource` returns nil for non-Fitbod (PhaseTraining/Imports/WorkoutCSVImporter.swift:67). Either implement header detection for both (small: header-prefix match, CRLF-tolerant — also fixes the byte-exact CRLF bug) or delete the dead cases and the promising copy. Prefer implement; the parse layer exists.
- [x] **T2-7 sessionsPerWeek cap inert; sessionMinutes ignored.** VERIFY: `generateWeek` has no app callers; `targetMovementCount` never reads `memory.sessionMinutes` (PhaseTraining/Data/SportSeason/SportSeasonGenerator.swift:25, :177). Minimal fix this cycle: clamp lift-day count by the phase rule in Planner (where liftDaysPerWeek is applied), and scale movement count by the user's sessionMinutes against the rule's band. Anything deeper is Tier 3.
- [x] **T2-8 SupportScheduler.schedule is test-only surface.** VERIFY: only `deconflictInPlace` is wired (PhaseTraining/Data/SportSeason/SupportScheduler.swift:53). Decide: wire placement into Planner's support pass (fixes T1-57 properly) or demote the header comment to describe reality. Wiring it is the fix that makes T1-57 structural — prefer that if the verify holds.
- [x] **T2-9 Prompt-injection sanitizer coverage gap.** VERIFY: session names, logged exercise names, and custom routine titles reach the prompt unsanitized (PhaseTraining/Coach/CoachContext.swift:279). Route all user-authored strings through `sanitizeFreeText`.
- [x] **T2-10 Unsequenced conversation saves can persist stale transcripts.** VERIFY the race (PhaseTraining/Coach/CoachConversationStore.swift:183). Serialize saves (single serial queue or actor).
- [x] **T2-11 GeneratorContext dead signals.** VERIFY: `recentSoreAreas`/`stagnantExercises`/`exerciseAffinities` have no consumer (PhaseTraining/Data/GeneratorContext.swift:30). These belong to the PARKED adaptive layer — do NOT delete; gate their computation behind a lazy accessor so regens stop paying for unread signals, and leave a pointer comment.
- [x] **T2-12 Onboarding can't resume.** VERIFY the hard-coded fresh start (PhaseTraining/Screens/Onboarding/OnboardingFlow.swift:56). Persist the draft to UserDefaults on step change; offer resume on relaunch.
- [x] **T2-13 Library segments: dead state + count queries.** VERIFY `detailExercise`/`memoryStore` unused (PhaseTraining/Screens/LibraryScreen.swift:37) — delete; replace the 582-struct count materialization with `SELECT COUNT(*)` (:119).
- [x] **T2-14 Custom-routine reps/sets clamps are silent; blank routine offers Delete.** (PhaseTraining/Screens/CustomRoutineEditSheet.swift:361, :45). Surface the bounds; require a name / suppress Delete on never-saved drafts.

## Tier 3 — architecture debt (REPORT ONLY this cycle)

Written to `audits/2026-08-23-architecture-report.md`. Headline items: the
per-render DB-query pattern (TodayScreen, TodayRecoveryCard,
ProgressRecoverySection, CompleteScreen, InjuriesEditor, LibraryScreen);
saveActive-per-keystroke + listSavedSessions N+M on main at launch;
ExerciseThumbnail's unbounded ~150MB UIImage cache; CoachContext's
13-parameter snapshot; conversation-history quadratic token growth; app-wide
Dynamic Type absence; UserDatabase error-handling model; component
duplication (FlowLayout×2, day-badge×3, parseRest×6, equipment-tier×2,
formatters); BodyMetricsEditor living inside an onboarding step file;
"Coach" naming collision between the SQLite DAO and the LLM subsystem.

## Tier 4 — UX polish (separate sitting; leave unchecked)

- [ ] Stale header comments batch: TodayScreen:5, PostWorkoutFeedbackSheet:4, SubstituteExerciseSheet:12, CheckInFeedbackScreen:1, CheckInPreviewScreen:1, InjuryPickerSheet:1, CoachContext:207+400, WorkoutGenerator:193, LibraryScreen:47 ("551-row"/"148 routines"), OnboardingPlanPreviewScreen:8.
- [ ] "Eight quick questions" vs 9 steps vs "STEP X OF 10" (OnboardingWelcomeScreen:28); progress-bar fill at both ends (OnboardingFlow:234).
- [ ] Modality label inconsistency `body_weight` vs `Body Weight` (TodayScreen+TemplateEditor:234).
- [ ] Unreachable "Resume workout" label + guard (TodayScreen:573).
- [ ] Completed-set row un-completes on whole-row tap (LogSetRow:110).
- [ ] SportPickerSheet lists user's sports twice (WeekDayEventSheets:33).
- [ ] Check-in keyboard dismissal (WeeklyCheckInFlow:252); dark-chrome gaps (ExerciseDetailSheet:37, HealthImportsScreen:26); one-entry anatomy legend (ExerciseDetailSheet:174); "1204d ago" (ProgressScreen+ExerciseCards:274); volume-card axis labels (StatCards:129); weight-change value-judgment colors (BodyCards:85); library tile info scent (LibraryScreen:274); detail-sheet dead end (ExerciseDetailSheet:30); bundled workouts browse-only (LibraryScreen:536) — this one is close to Tier 1 product value, promote next cycle.

## Cross-cutting

- **Accessibility**: zero VoiceOver affordances in Onboarding (OnboardingFlow:299), WeekScreen drag-only rescheduling (WeekDayEditSheet:226), Progress charts unlabeled (ProgressCharts:13), anatomy labels read raw slugs (BodyAnatomyView:128). Batch as its own sitting.
- **Dynamic Type**: every custom font is fixed-point, app-wide (Typography.swift:37, CoachDrawer:179, OnboardingFlow:164). Tier 3 report.
- **Error surfacing**: raw `\(error)` in 3+ places; no taxonomy. T1-39/T1-41 fix the instances; the pattern belongs in the report.
- **Destructive-without-confirm**: T0-6, T1-3, T1-8, coach CLEAR (T2-4). Same review lens for any new destructive action.
- **Parts never critiqued** (completeness critic): Paywall/entitlements as a flow, Mascot subsystem (Kettle/Repelican), Theme/design system + shared Components, App shell launch wiring. Next audit's parts list starts with these four.

## What NOT to touch (load-bearing)

- `CoachEntitlement.proRequired = false` — deliberate product decision (2026-06-05), coach ships free. Don't flip, don't "clean up" the held-open gate or the flip-day checklist comment.
- The PARKED adaptive layer (`GeneratorContext` signals, `build_workout` tool) — built, deliberately unconsumed. Gate, don't delete (T2-11).
- `--ui-test-*` / `--seed-*` launch args and their behaviors — XCUITest recipes depend on them (only move T0-10 inside DEBUG).
- UserDatabase migration runner + v1 migration list ordering — never edit/reorder past entries (the :157 house-style hazard goes in the report, not in a code change).
- WeekPlan's `.mobility` → `.rest` decode rewrite — decode compatibility for existing users.
- `sanitizeFreeText` and the coach prompt's cached static header — extend coverage (T2-9), don't restructure caching.
- Season engine as the ONLY generator — do not resurrect legacy-generator behavior while fixing T1-49..53 (see `phase-training-season-generator-engine-pitfalls`).
- Authored-routine serving path (`shouldServeAuthored`) — pilot-sport logic is deliberate.
- Hand-managed project.pbxproj — new files need the 4-edit dance; never regenerate it.

## Test coverage gate (before ANY Tier 3 refactor)

1. SessionStore save/load round-trip incl. bodyweight sets and warmup flags.
2. Backup snapshot→wipe→restore round-trip covering the T0-3 extended envelope.
3. `wireHistory()` contract: role alternation + empty-assistant filtering (the untested wire contract, CoachConversationStoreTests.swift:13).
4. Season-pool health across ALL plannable sports (ships with T0-7).
5. StrengthStandards matcher table test (ships with T1-20).

## Notes for the runner

- One commit per item, `T0-N`/`T1-N` in the message. Straight to main (no branch protection by design — see memory).
- Don't bundle across tiers. Don't fix Tier 4 "while in the file."
- Per-item: verify the finding's factual premise first; tick with `FALSE-POSITIVE:` note if it doesn't hold.
- Build gate after each group: unit tests via `xcodebuild test`; grep the full log on failure.
- Tier 3: report only. Tier 4: untouched this cycle.
