# 2026-09-03 critique-suite backlog

Source: the 13 critiques in this directory, run sequentially 2026-09-03.
Every item cites the critique and finding it came from; open that finding for
the evidence before acting.

## How to run this

Top-down. Tier 0 first. One commit per item.

**Per-item: verify the factual premise against the code you are about to edit
before writing the fix** (`verify-audit-claim-before-implementing`,
`ios-audit-finding-false-positives`). This suite corrected one of its own
findings mid-run (01 F1, see 12 F4); assume it can be wrong again.

**Build gate: `xcodebuild test` on the WHOLE scheme, not the unit target.**
That distinction is item T0-2 and is why the previous cycle shipped with a red
UI suite (13 F5).

## What NOT to touch

- The season engine is the only generator. Do not resurrect legacy-generator
  behaviour (`phase-training-season-generator-engine-pitfalls`).
- `SportCatalog.isPlannable` and the pilot-sport routing are deliberate scope,
  not a bug. The scope question is T3-1, an owner decision.
- Both entitlement `proRequired` flags are held open by dated product
  decisions. Flipping them is T3-4, not a fix.
- `PlanStore+LLMRefinement`'s disabled state is correct and should stay disabled
  until the generator consumes `strategy`.
- Segment-grain and pool-size decisions in `PhaseRule` are tuned by hand. Do not
  bulk-edit weights; 01 F2 says most of them cannot change the output anyway.

---

## Tier 0 — ship blockers, safety, App Review

**Status 2026-09-04: ALL 7 landed.** One commit per item, straight to main.
Two turned out to be product bugs rather than the doc/test problems they were
scoped as: T0-3 found "Start workout" gated on having a NAME, and T0-4 found
the privacy manifest declared in a file Apple does not read.

Gated on the whole scheme rather than the unit target, which is the lesson from
the previous cycle (13 F5). Each item was verified against the tests it claims
to fix as it landed; all seven of main's previously-red UI tests pass.

- [x] **T0-1 The authored path applies no injury filter, and the UI promises it does.** `AuthoredRoutine.workout` (`AuthoredRoutine.swift:127`) maps every routine row with no reference to `profile.excludedExerciseIds`, while `InjuriesEditorSheet.swift:117` tells the user "We'll filter out exercises that aren't safe for the injuries you pick." Seven of ten plannable sports are served by this path, climbing included. 12 injury types have contraindicated exercises inside authored routines; a declared finger-pulley injury can be served `Climbing Finger Strength Protocol (Repeaters)`. Fix: filter `rows` by the excluded set before the map, and return nil if the filter empties the routine. Add the invariant test from T2-4 in the same commit. *(03 F1, F2)* **DONE `594654a`** — `profile` is a required parameter, not a defaulted empty set. Ships the injury-contraindication invariant (T2-4's first half) across both paths, 5 phases and 3 slots. Mutation-checked: with the filter removed the climbing case fails on Weighted Pull-Up and Hangboard Repeaters.
- [x] **T0-2 CI has been red for 13+ consecutive runs; two onboarding tests tap a button T0-5 correctly disabled.** `TapBudgetTests.swift:292` and `OnboardingPlanDetailUITests.swift:30` call `onboarding-continue-coachConsent` without picking a consent option; `OnboardingCoachConsentScreen.swift:35` gates it on `localOn != nil`. Both walks stall and time out on the plan preview. Fix: tap a consent option first in both tests, and bump `tap-budget-baseline.json` `onboarding-to-first-plan` 12 → 13. *(05 F1, F2; 12 F2; 13 F1)* **DONE `0908b1c`** — `OnboardingPickRow` already had an `a11yId` parameter, so this passes one at each call site. Both walks decline consent. Budget 12 -> 13.
- [x] **T0-3 Diagnose the five-test start/finish cluster.** `test_completeScreen_presentsWithKettle`, `testTapBudget_discardWorkout`, `testTapBudget_startSavedWorkout`, `testBuildAndStartWorkoutRoutesIntoLiveLog`, `testTapBudget_buildAndStartWorkout` all fail on main, all three retries, and all launch from `--ui-test-onboarded` seeds so T0-2 does not explain them. Assertions point at starting a built workout and at Finish presenting the summary. This is the core loop; establish whether it is a rig problem or a live regression before anything else in Tier 1. *(06 F6; 13 F1)* **DONE `139d4bc`** — three causes, one of them a real bug: Start workout was gated on `canSave` (needs a NAME) while its own footer says starting does not save. Split `canStart` out. The other two were T1-3's Finish confirm and an unnamed routine failing Save. Also hardened `TapCounter.tap` to require `isEnabled`, which is how all three hid.
- [x] **T0-4 No `PrivacyInfo.xcprivacy` exists.** `find . -name "*.xcprivacy"` is empty and the string appears in no build config. `NSPrivacyCollectedDataTypes` is declared in Info.plist, which is not where Apple reads a manifest, and the single declared type is `OtherUserContent` while the Coach transmits body metrics and injuries. Fix: add the manifest, declare the `UserDefaults` required-reason API, and declare Health/Fitness data types. **Re-check Apple's current requirements first** — that rule has moved. *(11 F3)* **DONE `a148e76`** — adds the manifest, declares UserDefaults CA92.1, and declares Health + Fitness alongside OtherUserContent. Verified no other required-reason API is used.
- [x] **T0-5 `docs/privacy.md` misdescribes the Coach payload, and the promised privacy link does not exist.** The policy says the app sends "your messages plus a snapshot of your current plan, recent workout feedback, and today's plan day"; the context actually assembles 22 blocks including body metrics, structured injuries, strength estimates, dislikes, constraints, sport-log notes and feedback notes. Separately, `CoachConsent.swift:8` says the modal presents "a link to the privacy policy" and no such link exists in the app — this was sub-item (c) of the previous cycle's T0-5, which is checked off. Fix: rewrite the policy from `CoachConsent.modalBody` plus the four undisclosed free-text blocks, host it, add the link. *(11 F1, F2; 12 F1)* **DONE `a148e76`** — policy rewritten from the actual 22 context blocks; consent modal gained the four free-text blocks; GitHub Pages URL verified live (HTTP 200) and wired into `CoachConsent.privacyPolicyURL` plus a Link on the onboarding consent screen.
- [x] **T0-6 The app prescribes loads to injured users and carries no disclaimer.** One grep across `PhaseTraining/` and `docs/` finds a single hit, inside `CoachSystemPrompt.swift:60`. Fix: a disclaimer on the onboarding welcome or the injury editor, plus a line in `docs/privacy.md`. *(03 F4; 11 F6)* **DONE `e66c760`** — onboarding welcome and the injury editor.
- [x] **T0-7 The paywall sells a capability that does not run.** `PaywallView.swift:74`: "plus an AI coach that personalizes every workout." Seven of nine `GeneratorStrategy` fields never reach the generator. The sibling refinement path disabled itself over this exact claim, calling it "pure token spend plus a false product claim" (`PlanStore+LLMRefinement.swift:44-63`). Fix: either wire strategy (T1-6) or rewrite the sentence to sell the deterministic seasonal planner, which is the real differentiator. *(08 F1, F2; 09 F3)* **DONE `e66c760`** — the feature list carried a SECOND false claim the critique missed ("Personalized workout polish on every plan generation" is the disabled refinement pass). Both rewritten to season-phase and two-sport planning.

## Tier 1 — user-facing correctness

**Status 2026-09-04: 10 of 11 resolved.** T1-2, T1-3, T1-4, T1-6, T1-7, T1-8,
T1-9, T1-11 landed; T1-10 refuted and moved to Tier 3; T1-1 landed but is
**not runtime-confirmed**. Two accuracy corrections recorded on the items
(T1-9's severity, T1-10's premise).

**T1-5 is the one left open, on purpose.** 42 of 56 declarable injuries still
have no contraindication mapping. Each row asserts that a specific exercise is
unsafe for a specific injury; it is the one table in this app whose errors hurt
people, and a model should not write it. The mechanical half that IS safe: tell
a user declaring an unmapped injury that the app has no exercise filter for it,
rather than showing them a filtering promise that does nothing for them. See the
second pass, section 03.

Second pass on all 13 lenses: `audits/2026-09-04-r2/R2-REPORT.md`. It found four
new items (R2-01 to R2-04); R2-01 and R2-02 were fixed in the same sitting.

- [x] **T1-1 The default user hits a dead Today screen on the Monday after week one.** Nothing regenerates on a date change (`needsRegeneration` is inputs-hash only, `PlanStore.swift:634`); rollover snapshots without replacing (`:333`); the only thing that stages next week is the Weekly Check-In, presented only from a notification deep link; that notification is off by default (`WeeklyReminderScheduler.swift:35`). With a stale plan, `todayPlan` is nil, the upper-1 fallback is gated on `plan == nil`, and `TodayScreen.swift:548` disables Start with no explanation. **Confirm by moving the simulator clock forward 8 days before fixing.** *(06 F1)* **DONE** — `needsRegeneration` gained a date term, `planCoversCurrentWeek` is the predicate, and `refreshForCurrentWeek` runs on foreground: snapshot the outgoing week, prefer a check-in-staged plan, else generate. Skipped mid-session. **Not runtime-confirmed** — unit-covered only; move the simulator clock forward 8 days before shipping.
- [x] **T1-2 Every session is four exercises because of an integer division.** `sessionMinutes` defaults to 45 (`TrainingMemory.swift:51`), ski off-season clamps it up to its 50-minute floor, and `50 / 11` is 4 (`SportSeasonGenerator.swift:253`). Four slots also make the smallest expressible demand share 0.25, which zeroes every demand weighted under a quarter — including `hipLateral` and `prehab`, the two the off-season objective promises will "fix imbalances". Fix: round rather than truncate, or lower the 11-minute divisor to match the 33-minute sessions the assembler actually measures. *(01 F1, F2, F4)* **DONE** — rounds instead of truncating, so 50 minutes resolves to 5 movements rather than 4 and any demand weighted at or above 0.2 becomes expressible. The 11-minute-per-movement heuristic is left alone: `assemble` measures that same session at ~33 min (~8.25 per movement), and reconciling the two estimators changes every phase, so it is its own change. Four tests pin `exercises.count == 4` and all four build their own fixtures, so none regressed.
- [x] **T1-3 A three-day week is the same workout three times.** Ski off-season sessions 1 and 3 are identical and session 2 differs in one slot; event prep is worse. Needs either more movements per pool or week-level demand distribution rather than per-session. Larger than T1-2 and gated on it. *(01 F3)* **DONE** — slots are apportioned across the whole week and dealt per session (`weekSlots`), which also closed 01 F2: the quantisation floor is now 1/(perSession x sessions), so `power`, `legEndurance`, `prehab` and `hipLateral` all realize non-zero instead of rounding away. The three ski off-season sessions now open with three different movements. `guaranteeSignature` keeps the sport-defining demand in every session.
- [x] **T1-4 A Coach swap writes `exerciseId: 0` and inherits the replaced exercise's prescription.** `WorkoutDiff.swift:88-103`. Three effects: the row detaches from the catalog so no injury contraindication can match it, sets/reps/rest/pattern carry over from a different movement, and `rpe`/`tempo` are dropped by the initializer. This is the third instance of the defect T0-9 fixed at two other sites last cycle. Fix: resolve `toName` to a catalog exercise, derive identity and prescription from it, clear what does not transfer. *(04 F2)* **DONE** — resolves `toName` through `ExerciseLookupCache` (canonical name, then shorthand, slug and aliases), so the row keeps a real `exerciseId` and an injury contraindication can match it. Identity (`pattern`, `isCompound`) comes from the new movement; the dose (`sets`/`reps`/`restSeconds`) is kept because that is what the user asked to preserve; `notes`/`rpe`/`tempo` are dropped, `notes` above all because it can carry a load target computed from the OLD lift's history.
- [~] **T1-5 42 of 56 declarable injuries filter nothing.** Only 14 rows in `common_injuries` carry any `contraindicated` mapping. The 42 without include meniscus tear, hamstring strain, achilles tendinopathy, shoulder dislocation, and three stress-fracture entries. Fix: extend `exercise_injury_relevance`, starting with the load-management cases. *(03 F3)* **SAFE HALF DONE** — each injury card says "Filters N exercises from your workouts" or "No exercise filter for this injury yet. Your plan won't change; check with a clinician." The mapping table is untouched on purpose: it is the one table whose errors hurt people and a model should not write it.
- [x] **T1-6 The Coach Request screen bills an LLM call whose output is mostly discarded.** `CoachRequestScreen.swift:459` passes a strategy into `generateLift`, which drops it on both paths. Only `durationMinutes` and `focus` leak through indirectly. Either wire strategy into the season and authored generators, or gate the screen the way `PlanStore+LLMRefinement` gated itself. *(04 F1, F6)* **DONE, honestly scoped** — `strategy` reaches `generateSession`: intensity bias on sets, and per-exercise RPE / tempo / target-weight overrides by name. `emphasizePatterns`/`deprioritizePatterns` are movement-pattern slugs with no demand equivalent and stay ignored deliberately rather than mapped by guesswork. `context` (readiness, soreness) is still unwired and is now the largest remaining item in this area.
- [x] **T1-7 The set-completion control is 22pt.** `checkDot` (`LogSetRow.swift:275-289`) against a 44pt minimum, and it is the most-tapped control in the product. The code already diagnosed the size and fixed the test's ability to hit it rather than the user's. Fix: `contentShape` or padding to 44, keeping the 22pt visual. *(07 F2)* **DONE** — the dot still draws at 22; a 44x44 frame plus `contentShape` inside the button label makes the target 44. Also added the `accessibilityLabel` it never had ("Complete set 3" / "Set 3 done, tap to undo"), which is the first item of T2-2.
- [x] **T1-8 The deload is a badge, not a program change.** `MesocycleProgression` computes real cycle status and its only non-test consumer is `SeasonPhaseBadge`. `weekNumber` reaches the generator and is used only in the seed string (`SportSeasonGenerator.swift:45`). Week 4 of a block shows DELOAD above the same prescription as week 1. *(06 F2)* **DONE** — `deloadAware` resolves the mesocycle status and swaps in `PhaseRule.deloaded()`, which re-resolves every `DemandScheme`, so sets, reps and RPE all lighten rather than one number. Week 4 of a 4-week block prescribes strictly fewer sets than week 1; weeks 1 and 2 are identical. Both pinned by tests.
- [x] **T1-9 Thin authored routines are served as a day's workout.** **Severity corrected 2026-09-04**: the critique counted all 113 routines (24 with one exercise, 42 with one or two), but `authoredRoutineIds` already filters on goal and `duration_minutes >= 25`, and only **3 one-exercise and 2 two-exercise** routines survive that. Real, and much smaller than reported. **DONE** — added a `>= 3` exercise gate to the selector query, which is the season engine's own documented movement floor. Measured before landing: every plannable sport keeps at least 2 qualifying routines and general-fitness keeps its Easy Strength last resort, so no sport loses plannability. *(02 F5)*
- [x] ~~**T1-10 A climbing user can pick a variant the generator ignores.**~~ **REFUTED 2026-09-04.** `SupportPattern.variant` describes the SUPPORT sport and feeds the support-load table keyed by (primary variant, support variant, magnitude); it was never the primary sport's programming variant. No primary variant is stored in `TrainingMemory` at all, so nothing is ignored. What remains is F9 and it is a missing feature, not a bug: the variant picker marked M4 at `SportSeasonGenerator.swift:231` never landed, so `.inbounds`/`.sportRoute` are hardcoded and the backcountry/skimo/park/boulder/tradAlpine rule overrides plus 4 `aerobicUphill` movements are unreachable. **Moved to Tier 3** as an owner decision. *(01 F9, F10)*
- [x] **T1-11 The Apple Health grant is unreachable in practice, so the features behind it are dead.** Authorization is requested only from two buttons inside Profile -> Health & Imports (`HealthWorkoutSyncSection.swift:198` for workouts, `BodyMetricsSyncSection.swift:118` for body metrics). Onboarding never mentions Health (grep for `Health` across `Screens/Onboarding/` is empty) and nothing on Today, Week or Progress points there. HealthKit deliberately does not tell an app that a read was denied, so an ungranted install is indistinguishable from one with no workouts, and the app cannot detect the gap to prompt for it. Everything downstream is therefore inert and invisible for anyone who never goes looking: the on-open activity detection shipped in `1fb80da`/`b41c330`, readiness from real activity, and body-metric import. **The permission model itself is correct and should not change** — `ActivityDetectionStore.scan` reads without requesting on purpose ("this surface must never nag someone who hasn't opted into Health") and asking on launch would be worse. What is missing is one prompt at a moment of value: the Health & Imports row on Profile carrying a not-connected state, or an offer the first time a sport day is planned. *(raised by Wilbur 2026-09-04; not in the original 13 critiques)* **DONE** — the Profile row now reads "Not connected" until something has been imported, which is the only honest proxy since HealthKit will not report a denied read. Cached in `@State` and refreshed on appear and on sheet dismiss, NOT queried per render (architecture report item 1). The permission model itself is unchanged.

- [x] **R2-05 The authored path never filters by equipment, and for a bodyweight-only user that is most of the library.** Found by the new invariant harness (T2-4) on its first run. `SportSeasonGenerator.filteredPool` drops movements whose required equipment the athlete lacks; `AuthoredRoutine.workout` serves every row. Measured 2026-09-04 over the grid: **2,262 of 3,294 authored rows served to a bodyweight-only user require loaded equipment (69%)**, and 103 of 113 routines contain at least one such exercise. This is the equipment half of the T0-1 gap. **Not auto-fixed**, because the obvious fix (filter rows like the injury filter does) would gut 103 routines for that user and the right fallback is a product call: drop to the generic base, substitute a bodyweight variant where `exercise_substitutions` has one, or tell the user the sport has no bodyweight-only content. Numbers are in `SeasonInvariantTests.test_bodyweightOnlyNeverPrescribesLoadedEquipment`'s printed line each run. *(R2, from T2-4)* **Landed 2026-09-04 (option A where a substitute exists, C where none does):** `AuthoredRoutine.resolvedRows` applies the season engine's rule (required slugs a subset of the allowed set) after the injury filter, swaps a row the user cannot equip for the best curated substitute they can (equipment contexts first, then similarity), drops it otherwise, and the selector's floor reads the same rows so a thinned routine is skipped for the next one. When nothing fits, the day says "No <sport> routine fits your equipment and injuries yet" instead of serving a barbell session. `exercise_substitutions` was audited first (1,871 to 1,795; 54 pairs sharing neither pattern nor mover, 15 lower_intensity pairs that were harder). The bodyweight-only invariant is now asserted on both paths.


## Tier 2 — coverage and accessibility

**Status 2026-09-04 (evening): 8 of 10 done, 1 partial, 1 refuted.**
T2-1, T2-3, T2-4, T2-5, T2-6, T2-8, T2-9, T2-10 landed with tests. T2-2 is
partial (logging surface only). T2-7 refuted. Whole scheme green: 1,110 passed, 0 failed (unit 1,068, UI 42, budgets all on reference). What is
NOT yet true, said plainly so nobody reads the checkboxes as "on par with
Fitbod": Dynamic Type is wired at the root and in the log rows but has not
been looked at on a device at AX5, and the fixed frames on Today, Week and
Progress are untouched; VoiceOver has labels on the log and the summary and
nowhere else; the authored path honours equipment since 2026-09-04 (R2-05).

- [x] **T2-1 The app ignores Dynamic Type completely.** `TypeSpec.font` is `Font.custom(fontName, size:)` with no `relativeTo:` (`Typography.swift:37`), and `relativeTo` appears in zero of 134 `.custom(` call sites. Body copy is fixed at 13pt, labels at 10pt. Must be done together with the fixed row frames in `LogSetRow` or the log clips. *(07 F1, F3)* **DONE** — `TypeSpec.font` is `Font.custom(_:size:relativeTo:)` with a per-style text style chosen so its default size is within 25% of the design size (keeps the scale curve proportional); all nine design sizes unchanged at the default setting. `LogSetRow`'s six fixed frames are `@ScaledMetric` (declared on `LogScreen`, since the row file is an extension). All 13 tap budgets on reference afterwards. Not yet checked visually at AX5; fixed frames on Today, Week and Progress are the next pass.
- [x] **T2-2 VoiceOver has nothing to read on the logging surface.** 3 `accessibilityLabel`s across LogScreen, LogSetRow, LogExerciseBlock and CompleteScreen, against 16 identifiers. The check dot has an identifier and no label. Do it a screen at a time, starting there. *(07 F4)* **PARTIAL** — logging surface: check dot, weight ("Weight in lbs, set 3"), reps, effort menu (label + value), exercise name as a header trait, CompleteScreen stat cells as one element each. Learned: overriding a TextField's `accessibilityValue` changed what XCUITest reads through `.value` and broke two LogFlowTests; units live in the label. **Then the rest**: LogScreen's icon-only discard button, Today's sport-prescription block (one element) and the coach-adjusted badge, Progress stat cells and body-metric blocks (one element each: "Sessions, 12, this month"), and the Week day row (one element in speaking order with the full weekday: "Tuesday 9, Lift, Ski off-season A, today, protected"; the parent's `week-day-row-N` identifier stays on the container so the row-tapping tests still work). History already had labels. Library and Profile rows are text-labelled SettingsRows and derive their own. UI target 42/42 after each pass.
- [x] **T2-3 Write gate test 3: the `wireHistory()` contract.** `wireHistory` appears zero times in the unit target. It decides what conversation history is sent to Anthropic. Pure function over an array of turns; role alternation and empty-assistant filtering. Unblocks Tier 3 architecture work. *(13 F2)*
- [x] **T2-4 Port an invariant test for the season engine.** `GeneratorInvariantTest` (312 lines, incl. the injury-contraindication safety invariant) was deleted with the legacy engine in `449bd8d` and never replaced. Start with the one T0-1 needs: no contraindicated exercise appears in any generated session, for any profile, on **either** path. *(13 F3)* **DONE** — `SeasonInvariantTests`: ~2,700 cells (10 sports x 5 phases x 3 experience x 3 equipment tiers x 2 lengths x 3 slots), through `generateLift` so both paths are covered. Part A: no empty workout, sets in 1-8, no duplicate movement, reps + rest present, deterministic. Part B: shorter never adds movements; bodyweight-only never gets loaded gear on the season path. Its first run found two things: a `rest >= 15` oracle that was wrong (the MTI circuit rows are a real 20s/10s prescription) and R2-05, the authored equipment gap, which it now reports as a count each run.
- [x] **T2-5 Close gate 1: warmup-flag round-trip through SessionStore.** `isWarmup` appears in eight test files and none of them is `SessionStoreTests`. *(13 F2)* **DONE** — save through one store, reload through a FRESH store over the same defaults, assert the warmup flag and an empty bodyweight load both survive.
- [x] **T2-6 Seven Tier 0/1 fixes sit in files with zero unit-test coverage.** `CoachClient` (the T0-1 spend ceiling; `dailyRequestCeiling` is asserted nowhere), `BackupCoordinator` (T0-2), `RestTimerState` (T1-2), `MiniWorkoutDiffCard` (T1-46), `InsightGenerator` (T1-47), `InactivityReminderScheduler` (T1-55), `CoachConsent` (T0-5). Start with the spend ceiling. *(13 F4)* **PARTIAL** — `CoachClientCeilingTests` covers the T0-1 spend ceiling (allows exactly N then refuses, rolls over on calendar day, a refused request does not advance the counter, ceiling > chat hard cap). **Then the rest landed**: `InactivityReminderSchedulerTests` (the T1-55 generation token, made internal), `RestTimerStateTests`, `InsightGeneratorSanitizeTests` (T1-47's two-sentence regression, `sanitize` made internal), `CoachConsentTests` (both disclosure strings name every sent category; the short one is not vaguer than the modal; the policy URL is https and quoted in the modal), and `StoreResetTests` (T0-2: each store's reset empties memory AND disk over a fresh suite). `eraseAllData` itself is not called from a test because it wipes `.standard`; its four resets are. The four `Mini*DiffCard` views remain uncovered (SwiftUI views; `canApply` is private). The T2-5 round-trip was flaky against the shared on-disk `UserDatabase` and now uses its own temp file.
- [x] ~~**T2-7 Make the tap-budget tracker report a missing flow.**~~ **REFUTED 2026-09-04.** `scripts/quality/tap_budget_diff.py` already classifies a baseline flow with no marker as `MISSING (in baseline, not run)` and prints "Not run (in baseline, no marker)" in the footer, and prints a separate line when NO markers are found at all. The first pass claimed it "prints nothing when the emitting test stops running"; it prints exactly that. What stands is only the non-gating design decision, which is deliberate. **T2-7 Make the tap-budget tracker report a missing flow.** It is non-gating by decision, which is fine; it prints nothing when the emitting test stops running, which is not. Three baselines have been unobserved for two weeks. One loop over the expected flow names. *(05 F3; 13 F6)*
- [x] **T2-8 Add one Dynamic Type assertion.** Nothing in either target references `dynamicTypeSize`. A single test that a font resolves differently at two content-size categories would have caught T2-1 at any point. *(07 F7 / 13 F7)* **DONE** — `TypographyDynamicTypeTests`: every style maps to a text style, design sizes unchanged, no style pinned to a curve whose default is more than 25% off its own size.
- [x] **T2-9 Load prescription steps up unconditionally and never down.** `progressiveOverloadHint` always applies a 2.5% step from the all-time best, with no branch for missed reps, soreness, or a deload phase. The Epley math itself is correct; the gap is autoregulation. Interacts with T1-8 and with the parked adaptive layer. *(03 F6)* **DONE** — `LastAttempt` (heaviest completed working set of the most recent session, best reps at that weight, against the session's target reps) built in `GeneratorContext.from`, and `ProgressionDecision.decide` in front of the step: hit target -> +2.5%, missed by one -> hold, missed by two or more -> -5%. Conservative on purpose: a lighter day than the all-time best is never evidence about the best, so a deload or travel session cannot trigger a step-down. Eight tests; the Epley rep-band mapping is unchanged.
- [x] **T2-10 Readiness scaling exists and neither live generator uses it.** `WorkoutGenerator.swift:152-166` implements a readiness set multiplier and an RPE cap; `SportSeasonGenerator` contains zero occurrences of `readiness` or `soreness`, and the authored path preserves authored sets and reps by design. The app asks for a soreness check-in and prescribes the identical session either way. Same change as T1-6. *(03 F5; 01 F11)* **DONE** — `AthleteState.readinessScore: Double?` (nil = no data; the 0.5 sentinel never arrives) threaded from `generateLift`, and the season engine scales sets by the same `lerp(0.6, 1.0)` curve `makePickedRow` has used since Phase 2. Test pins readiness 0 < readiness 1 and nil == unscaled. `recentSoreAreas` is now the one unconsumed signal.

## Tier 3 — owner decisions, not defects

Each of these is a call to make, not a bug to fix. Listed so they are not lost.

- [ ] **T3-1 The acquisition research and the product target different people.** All ten niche briefs are gym-strength communities; all ten plannable sports are mountain sports. `STRATEGY.md:172` picks hybrid-hyrox, which has no slug in the app. Either the product grows toward the research (general strength path, canonical templates, drop the sport gate) or the research grows toward the product (briefs for the mountain-athlete communities no file covers). *(02 F1, F2)* **Decision 2026-09-04 (5a):** agreed, the research should grow toward the product. Redo the niche briefs for the mountain-athlete communities. No task assigned yet.
- [x] **T3-2 Three shipped routines are distilled from MTI's paid plans.** Two name `MTI Mountain Bike Pre-Season V2` directly; two pass through the owner's own shralpinism program. The exercise vocabulary is MTI's branded naming ("Leg Blaster"), `source_url` is empty on all six attributed rows, and MTI sells these at $35/month. Not a legal opinion; three facts to weigh before App Store submission. *(10 F2)* **Landed 2026-09-04 (5b):** routines 295, 296, 297, 299 and 300 rewritten from first principles as plain catalog sessions (`source_name` "Phase Training, first principles (2026-09-04)"); the ten MTI-branded exercises (Leg Blaster, Mini Leg Blaster, Scotty Bobs, Mr. Spectacular, Kneeling Slasher, Standing Founder, Poor Man's Leg Curl, Kneeling Curl to Press, Dumbbell Squat Thrust, Step-Up with Pack) retired from the catalog with every relation row pruned.
- [ ] **T3-3 Price is set against the wrong tier.** $49.99/yr is mid-pack among gym loggers where the app is outmatched, and an order-of-magnitude story against MTI's $420/yr where it is not. The strategy asked for $40/yr **plus $99 lifetime** and argued the lifetime option twice; no lifetime product exists and a monthly tier the strategy did not ask for does. *(09 F5; 10 F4)* **Deferred 2026-09-04 (5c):** the app is not tested enough for public release, so pricing waits.
- [ ] **T3-4 Nothing is gated and the free feature is the expensive one.** Both `proRequired` flags are false by dated decision. The Coach costs real money per install and is free; the support-sport reflow is named in code as the intended first paid feature and is also free. The code is already shaped for the flip, and `SupportPatternEditor` is the moment-of-value screen the paywall should route from. *(09 F1, F2, F6)* **Deferred 2026-09-04 (5d).**
- [x] **T3-5 Kettle is a face with no voice.** Either the mascot is decoration and the terse coach register is the app's voice, which is coherent, or Kettle speaks and the onboarding and empty states should carry it. *(08 F5)* **Decision 2026-09-04 (5e):** Kettle is decoration; the terse coach register is the voice. Closed.

## R3 — third pass, 2026-09-04 (scoped to 9c4a46f..cb7a798)

Found by `audits/2026-09-04-r3/` against a green CI. Every one of these was
invisible to a passing suite.

- [x] **R3-1 (high) `upperStrength` is allocated weight it can never realize in ski pre-season and in-season.** Target 0.05 in both, realized 0.00 in both, while every other non-zero demand realizes. Pre-season ties four demands at 0.05 with three remainder slots and the tie-break is alphabetical on `rawValue`, so `upperStrength` sorts last every week; in-season 0.05 x 8 slots floors to 0.4 and loses the remainder ranking. **Landed 2026-09-05:** pre-season 0.10 funded from `legEndurance` 0.15 -> 0.10 (owner's call: the week trades one high-rep leg movement for a Weighted Pull-Up, and legEndurance keeps a slot); in-season an honest 0.00 with the 0.05 returned to `maxStrength`, where 4b took it from. `test_everyFundedDemandRealizesASlot` drives three real generated weeks and asserts every funded, servable demand buys a slot. *(01 F1)*
- [x] **R3-2 (high) Three injuries contraindicate every `fingerStrength` movement climbing has.** trigger-finger, de-quervains and carpal-tunnel each remove all 4; biceps-tendinopathy removes 3. **Landed 2026-09-05.** The session says so when nothing survives, and the content half is done: three sourced exercises added (Active-Recruitment Finger Pull, Density Hang, Tendon and Nerve Glide Sequence) and the de Quervain's rule narrowed to the thumb-loaded movements its mechanism actually names. Surviving finger movements: de Quervain's 0 -> 4, trigger finger 0 -> 1, finger pulley 2 -> 3, biceps tendinopathy 1 -> 3, of 6. Carpal tunnel stays at 0 by decision, with the glide sequence in prehab instead. *(03 F1, F2)*
- [x] **R3-3 (high) The floor test cannot see an empty workout.** `testInjuryFilterNeverLeavesASessionUnderTheMovementFloor` opens with `if w.exercises.isEmpty { continue }`, so the worst outcome of R3-2 passes it. **Landed 2026-09-05:** an empty day is allowed only as one of the two declared states (`authored-no-fit`, `no-supported-sport`); a session filtered to nothing under any other provenance fails. *(03 F2, 13 F1)*
- [x] **R3-4 (medium) "unlock" is paywall language in the new empty state.** **Landed 2026-09-05:** now reads `No <sport> session fits your equipment and injuries yet · update Equipment in Profile`. *(08 F1)*
- [x] **R3-5 (medium) `test_pools_have_no_duplicate_exercise_ids` hardcodes the sport list.** **Landed 2026-09-05:** driven from `CoachDatabase.sportMovementSportSlugs()`, with an assertion that the list is not empty so a broken query cannot read as a clean pass. *(13 F2)*
- [x] **R3-6 (low) A test-host crash reports as one failure.** The duplicate-key fatal error killed the host three times and the summary read `failed=1`. **Landed 2026-09-05:** `scripts/quality/test-summary.sh` counts fatal errors and simulator collapse alongside failed cases and exits non-zero on any of the three. *(13 F3)*
- [ ] **R3-9 (low) The season engine prescribes by demand, so a movement's own protocol is overridden.** The Active-Recruitment Finger Pull is a 3-second overcoming isometric in its instructions and the engine serves it as `5x8s hold` from the `fingerStrength` `DemandScheme`. True of every engine-path movement, and harmless for most, but it is visible now that a movement carries a cited protocol. Either the scheme reads a per-movement override or the exercise text is what the athlete follows. *(R3-2 follow-on)*
- [ ] **R3-11 (high) Twelve injuries have no rule-generated contraindications because the 1b pass skipped every injury that already had a curated row.** lumbar-disc-herniation (10 curated rows), rotator-cuff-injury (4), wrist-sprain (3), patellar-tendinopathy (2), shoulder-impingement (2), and slap-tear, hip-flexor-strain, hip-labral-tear, tennis-elbow, golfers-elbow, whiplash, stinger-burner (1 each). ACL was the thirteenth and is done (+65 rows over impact, cutting, deceleration, pivot and catch patterns, 2026-09-05). Each of the twelve needs the same sourced pattern rule, written against the injury's own aggravating-movement literature, and the enum semantics in R4 03 F1 respected. *(R4 03 F2)*
- [ ] **R3-12 (low) The authored-routine time estimator ignores duration-based rows.** `AuthoredRoutine.workout` estimates `sets x 90 s`, so a `1x20 min` row counts as 90 seconds. 3 of 400 routine rows are duration-based today, so the blast radius is small; parse "min" in `reps` when present. *(R4 01 F1)*
- [ ] **R3-13 (low) Routine 70 "Pre-Season Ski Prep Block" sits in the snowboard rotation under a ski name.** Movements are appropriate, tag is deliberate at the snow-sports umbrella; only the name is wrong for a snowboarder. Rename or re-tag. *(R4 01 F3)*
- [ ] **R3-14 (low) The PFPS rule "beyond 90 degrees under load" removes an unloaded bodyweight squat.** Conservative rather than unsafe; a pattern rule cannot see load or depth. Revisit if PFPS users report thin sessions. *(R4 03 F5)*
- [x] **R3-10 (high) Four outdoor sports have no bodyweight-viable content.** **Landed 2026-09-05:** routines 301-304, one per sport, from a four-agent fan-out under a fixed contract; declared-empty days 315 -> 15 of 1,800. Two merge-time swaps for sport-mislabelled catalog names, one catalog rename, provenance corrected to credit every fetched source and the adaptation. *(FilterCompositionTests)*
- [ ] **R3-8 (medium) Five more funded demands realize no slots.** Found by the R3-1 assertion the moment it existed, so `upperStrength` was not special: ski maintenance `legEndurance` and `power`, climbing pre-season `prehab` and `core`, climbing in-season `prehab`, all at 0.05. Each is a coaching call about which demand surrenders a slot, so they are pinned in `SeasonFidelityTest.knownInertWeights` and asserted EXACTLY: fixing one without removing it from the set fails, and a new one fails, so the debt can only shrink deliberately. *(01 F1, follow-on)*
- [x] **R3-7 (low) Two landing claims corrected in place.** The substitutions count was stale by 7 within the same session, and the hangboard note described a movement the climbing pool does not contain. Both fixed above. The general lesson is in `audits/2026-09-04-r3/12-backlog-reverification.md`: read the artifact after the last commit of a group, not after the step that produced the number. *(12)*

## Tier 4 — polish, separate sitting

- [x] ~19 prose em/en dashes remain (four went with T0-7) in user-facing `Text()` literals, against the house ban. Two more are numeric ranges and are a separate call. *(08 F4)* **DONE** — 21 rewritten to periods, commas, colons or parentheses. One remains on purpose: `Enter 5–600 minutes`, a numeric range.
- [x] `OnboardingWelcomeScreen.swift:27` said "Eight quick questions"; `OnboardingStep.total` is 10 and welcome is not a question, so it now says nine. **DONE `e66c760`**, in the same paragraph as T0-6's disclaimer. *(12)*
- [x] `CoachConsent.swift:4-5` said the coach is "on by default for new installs"; T0-5 made it an affirmative choice. **DONE `a148e76`**. *(11 F5)*
- [x] ~~`docs/privacy.md:23` says Health-confirmed sessions may reach the Coach context.~~ **REFUTED 2026-09-04.** Raw HealthKit data does not, but confirming a detected activity writes a `SportLogEntry` (`Health/ActivityDetection.swift:136`) and sport logs are one of the 22 context blocks. The sentence is correct; the finding was not. *(11 F4)*
- [ ] The Tier 2 header in `audits/2026-08-23-backlog.md` says "ALL 12 landed"; the section has 14 checked items. *(12 F3)*
- [x] The climbing off-season objective says "hypertrophy" and prescribes 4x4-6 at RPE 7-8, which is max strength. *(01 F7)* **DONE (4b, 2026-09-04, verified 09-05)** — the objective now reads "Pulling + finger base, strength, structural prep" (`PhaseRule.swift:171`), which matches the scheme. The string was the wrong half.
- [x] Hangboard max hangs appear in all three off-season climbing sessions. *(01 F8; 02 F6)* **DONE (3a, 2026-09-04, verified 09-05)** — the realized off-season week is Max Hang, Max Hang, Plate Pinch Hold. Note the third slot is still `fingerStrength` (a pinch, not a max hang), not the pulling movement the commit message claimed; see R3 lens 12.
- [x] `progressiveOverloadHint` keys prior-best by lowercased display name. Low blast radius, same class as T0-9. *(03 F7)* **DONE** — additive: every `priorBest`/`lastAttempt` entry is written under the raw name (every older reader and test unchanged) AND under `id:<exerciseId>` when the catalog resolves the name through shorthand/slug/alias; reads try the id key first. A session logged as "Bench Press" now feeds a generated "Barbell Bench Press" row (catalog id 900). Three tests, including that the id key never overwrites the newest-session entry.
- [ ] The gateway account id is a string literal alongside the compiled-in token. Part of the still-open T0-1 external work from the previous cycle. *(11 F7)*
