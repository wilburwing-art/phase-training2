import SwiftUI

struct ContentView: View {
    var body: some View {
        ZStack {
            Color.black.ignoresSafeArea()
            Text("Phase Training")
                .foregroundStyle(.white)
        }
    }
}

#Preview {
    ContentView()
}
