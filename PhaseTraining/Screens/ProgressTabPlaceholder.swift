import SwiftUI

struct ProgressTabPlaceholder: View {
    var body: some View {
        TabPlaceholder(
            eyebrow: "PROGRESS",
            title: "Trends + PRs.",
            subtitle: "Coming in Phase 12 — volume, frequency, per-exercise charts."
        )
    }
}

#Preview { ProgressTabPlaceholder() }
