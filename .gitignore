# Xcode
build/
DerivedData/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/
*.xccheckout
*.moved-aside
*.xcuserstate
*.xcscmblueprint
.swiftpm/

# xcodegen output (regenerated from Project.yml)
*.xcodeproj

# Pre-build-generated secrets (CoachSecrets.swift comes from scripts/generate-coach-secrets.sh)
PhaseTraining/Generated/

# Swift Package Manager
Packages/
Package.pins
Package.resolved
.build/

# CocoaPods (not using, but defensive)
Pods/

# Carthage (not using, but defensive)
Carthage/Build/

# fastlane (Phase 5)
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# macOS
.DS_Store

# Claude Code local settings (per-machine permission overrides)
.claude/settings.local.json

# Generated build artifact from scripts/db/build_db.py — the canonical
# bundled DB lives at PhaseTraining/Resources/coach.db. db/coach.db is
# just a scratch file the build script writes before copying.
db/coach.db

# Quality-pipeline scratch: downloaded candidate thumbnails. The
# JSONL provenance files in db/quality/ ARE committed; the WebPs are
# temp working files. See PLAN-image-quality.md.
db/quality/candidate_thumbs/

coach.db.backup-*
.derived-integration/
.xcodebuildmcp/
