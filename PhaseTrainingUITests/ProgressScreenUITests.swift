// ProgressScreenUITests.swift — SCAFFOLD (item 7 of 7).
//
// No UI test currently touches the Progress tab, so a card that stops
// rendering ships silently. Smoke-level only: reach the tab, assert the card
// stack is present, and exercise the one interactive affordance
// (progress-see-all-sessions → HistoryScreen).

import XCTest

final class ProgressScreenUITests: XCTestCase {

    func test_progressTab_rendersCardStack() { XCTFail("scaffold") }
    func test_seeAllSessions_opensHistory() { XCTFail("scaffold") }
    func test_emptyState_shownForFreshInstall() { XCTFail("scaffold") }
}
